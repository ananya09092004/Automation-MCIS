1. One class per responsibility.

2. No platform imports another platform.

3. No LLM calls inside Desktop Platform.

4. Every public function must have type hints.

5. Every module must have tests.

6. Every module must define custom exceptions.

7. Data models stay in models.py only.

8. Interfaces never contain implementation.

9. Controllers implement interfaces only.

10. Every feature must pass Definition of Done before moving forward.